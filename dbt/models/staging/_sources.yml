version: 2

sources:
  - name: raw
    description: load_data_to_duckdb.py가 원본 파일을 가공 없이 적재한 raw 레이어
    schema: main
    tables:
      - name: raw_orders
        description: orders.csv 원본. 모든 컬럼이 VARCHAR로 적재된다.
      - name: raw_user_logs
        description: user_logs.jsonl 원본. event_properties가 누락된 행이 존재한다.
